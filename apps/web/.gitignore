# Next.js build output and generated types (next-env.d.ts is written by `next dev`/`next build`).
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# The Worker built by @opennextjs/cloudflare, and wrangler's local state (dev KV, Durable Objects).
/.open-next/
/.wrangler/

# Local variables and secrets for `wrangler dev`; production values are Worker secrets.
.dev.vars
.dev.vars.*

# Vitest
/coverage
