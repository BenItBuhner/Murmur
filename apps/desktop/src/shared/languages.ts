/**
 * Dictation languages. The table lives in the text engine so the desktop app, the Convex gateway
 * and (by way of the golden test) the Android app agree on codes, names and the prompt they build.
 */
export {
  AUTO_LANGUAGE,
  LANGUAGES,
  LANGUAGE_OPTIONS,
  languageName,
  languageLabel,
  type Language
} from '@engine/languages'
